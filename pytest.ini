[pytest]
# Pytest configuration for synth-gen

testpaths =
    src/synth_gen/data/tests
    src/synth_gen/models/tests
    src/synth_gen/evaluation/tests
    src/synth_gen/sweep/tests

python_files = test_*.py

python_classes = Test*

python_functions = test_*

addopts =
    -v
    --tb=short
    --strict-markers
    -ra

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
